---
interface Props {
  show: string;
}

const { show } = Astro.props;
---

<div
  class="absolute flex justify-center items-center h-full w-full bg-gray-50 top-0 left-0"
  x-show={`${show}`}
>
  <div class="loader"></div>
</div>
