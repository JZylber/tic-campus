---
import SectionLayout from "../SectionLayout.astro";
interface Props {
  subject: string;
  course: string;
}

const { subject, course } = Astro.props;
---

<SectionLayout name="home">
  <header class="flex justify-between gap-x-4">
    <h1 class="text-[60px] font-bold m-0 max-w-[75%]" x-text={`'${subject}'`}>
    </h1>
    <div
      class="self-stretch flex flex-col justify-center gap-y-4 min-w-[175px] w-[250px]"
    >
      <a
        class="rounded-xl bg-sh-purple p-2 flex justify-center items-center gap-x-4 hover:shadow-lg cursor-pointer"
        x-bind:href={`await getCourseLink('${course}')`}
      >
        <span class="material-symbols-outlined text-[28px]">groups</span>
        <span class="text-[20px]">Grupo</span>
      </a>
      <a
        class="rounded-xl bg-sh-purple p-2 flex justify-center items-center gap-x-4 hover:shadow-lg cursor-pointer"
        href="https://cheatsheets-nv.vercel.app"
      >
        <span class="material-symbols-outlined text-[28px]">note_stack</span>
        <span class="text-[20px]">Cheatsheets</span>
      </a>
    </div>
  </header>

  <p class="text-gray-600 indent-8 text-[20px]">
    ¡Bienvenidos! Este es el campus de la materia. Acá vas a encontrar todas las
    actividades, las clases dadas y más material. Además, si sos un estudiante
    de este curso, podés ver tus notas y tu progreso en la materia. ¡Explorá el
    menú del costado para ver más!
  </p>
  <div class="w-full max-w-[800px] self-center">
    <div
      style="
        position: relative;
        width: 100%;
        height: 0;
        padding-top: 56.25%;
        padding-bottom: 0;
        box-shadow: 0 2px 8px 0 rgba(63, 69, 81, 0.16);
        margin-top: 1.6em;
        margin-bottom: 0.9em;
        overflow: hidden;
        border-radius: 8px;
        will-change: transform;
      "
    >
      <iframe
        loading="lazy"
        style="
          position: absolute;
          width: 100%;
          height: 100%;
          top: 0;
          left: 0;
          border: none;
          padding: 0;
          margin: 0;
        "
        x-bind:src={`await getSubjectPresentation('${subject}')`}
        allowfullscreen="allowfullscreen"
        allow="fullscreen"
      >
      </iframe>
    </div>
  </div>
</SectionLayout>
