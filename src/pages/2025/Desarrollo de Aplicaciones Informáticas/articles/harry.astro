---
import CodeBlock from "../../../../components/articles/code-block/CodeBlock.astro";
import InlineCodeBlock from "../../../../components/articles/code-block/InlineCodeBlock.astro";

export const partial = true;
---

<h2>Instalación</h2>

<p>
  Necesitan pytest para correr los tests. Pueden instalarlo con requirements.txt
</p>

<CodeBlock lang="bash" code="pip install -r requirements.txt" />

<h2>Introducción</h2>
<p>
  En el mundo de Harry Potter, el hechizo <em>Avada Kedavra</em>, mata al
  oponente. Sin embargo, mediante el uso de <em>Horrocruxes</em>, el hechicero
  divide parte de su alma y puede revivir mientras tenga alguno. Es decir, un
  hechicero puede revivir solo si tiene algún <em>Horrocrux</em> disponible.
</p>
<h2>Consigna</h2>
<p>
  Crear la clase <em>Mago</em> (en el archivo <em>harry.py</em>) que:
</p>
<ul>
  <li>Se inicialice con el nombre</li>
  <li>
    Tenga un método <InlineCodeBlock
      code="crear_horrocruxes(numero: int)"
      lang="python"
    /> que cree el número pasado por parámetro de <em>horrocruxes</em>. Para
    pensar: ¿Los
    <em>horrocruxes</em> deben ser guardados fuera o dentro de la clase mago?
  </li>
  <li>
    Tenga el método <InlineCodeBlock code="morir()" lang="python" />. Si no
    tiene <em>horrocruxes</em>, morirá. Si tiene <em>horrocruxes</em>, restará
    un <em>horrocrux</em> de su total.
  </li>
  <li>
    Tenga el método <InlineCodeBlock code="vivo() -> bool" lang="python" /> que devuelva
    si está vivo o no (debe retornar True o False).
  </li>
  <li>
    Tenga el método <InlineCodeBlock
      code="avada_kedavra(mago enemigo)"
      lang="python"
    /> que mate al mago enemigo (<strong>TIP:</strong> usar el método morir del mago
    enemigo).
  </li>
</ul>
<p>Prueben que ande corriendo los tests con:</p>
<CodeBlock lang="bash" code="python -m pytest" />
<h2>Datos de color</h2>
<p>
  J.K Rowling creó otras escuelas de magia además de Hogwarts. Sin embargo, se
  olvidó de algunas escuelas yanquis, como esta:
  <a href="https://www.youtube.com/watch?v=j-2ZxldMO-M"
    >https://www.youtube.com/watch?v=j-2ZxldMO-M</a
  >.
</p>
