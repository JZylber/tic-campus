---
import OfferingsTable from "../../components/dashboard/offerings/OfferingsTable.astro";
import DashboardLayout from "../../layouts/DashboardLayout.astro";
---

<script>
  import { requireAuth } from "../../scripts/auth";
  import { rolesFor } from "../../scripts/dashboardSections";
  requireAuth(rolesFor("materias"));
</script>

<DashboardLayout>
  <div class="w-full h-full flex flex-col items-center overflow-y-scroll">
    <h1 class="text-[30px] md:text-[36px] font-bold pt-4 pb-2">Materias</h1>
    <h2 class="text-[16px] md:text-[20px] pb-4">
      Administrá las materias obligatorias y optativas y los cursos a los que se dictan.
    </h2>
    <OfferingsTable />
  </div>
</DashboardLayout>
