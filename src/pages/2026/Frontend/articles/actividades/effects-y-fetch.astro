---
import ImportantBox from "../../../../../components/articles/alertbox/ImportantBox.astro";

export const partial = true;
---

<h2>F1 Visualizer — useEffect y fetch</h2>
<p>
  En este TP van a completar una app React + TypeScript que muestra equipos y
  pilotos de Fórmula 1. La estructura, los componentes y los estilos ya están
  listos — solo tienen que implementar los <strong>fetch con <code>useEffect</code></strong> y
  renderizar las cards en los lugares marcados con <code>// Completar</code>.
</p>

<h2>¿Cómo arrancar?</h2>
<p>
  El proyecto se entrega a través de <strong>GitHub Classroom</strong>. Al aceptar
  la asignación se va a crear automáticamente un fork del repositorio en tu cuenta.
</p>
<ol>
  <li>
    Aceptá la asignación desde este link:{" "}
    <a
      href="https://classroom50.org/Avanzado-FrontEnd-4to/avanzado-frontend-2026/assignments/useeffect-y-fetch/accept"
      target="_blank"
      rel="noopener noreferrer"
    >
      classroom50.org — Aceptar asignación
    </a>
  </li>
  <li>Cloná el repositorio que se creó en tu cuenta.</li>
  <li>Corré <code>npm install</code> y luego <code>npm run dev</code>.</li>
</ol>

<ImportantBox>
  <p>
    <strong>Leé el <code>CONSIGNA.md</code> y el <code>README.md</code> del repo</strong> antes de empezar —
    tienen todos los detalles, pistas y descripción de la API.
  </p>
</ImportantBox>
